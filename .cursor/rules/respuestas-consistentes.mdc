---
description: Mantener un formato de respuesta API consistente en todos los controladores
globs:
  - "**/controllers/**/*.ts"
  - "**/use-cases/**/*.ts"
alwaysApply: true
---

## Respuestas Consistentes

Estandarizar la forma de las respuestas para todos los endpoints.

### Formato recomendado

```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  message?: string;
  errors?: any[];
  timestamp: string;
}
```

### Transformador/Interceptor

```typescript
@Injectable()
export class ResponseTransformInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map((data) => ({ success: true, data, timestamp: new Date().toISOString() })),
    );
  }
}
```

Aplicar globalmente o por controlador según convenga.

